{
  "framework": null,
  "installCommand": "pnpm install",
  "buildCommand": "REPO=$(git rev-parse --show-toplevel) && (cd $REPO/shared && pnpm run build) && (cd $REPO/client && pnpm run build)",
  "outputDirectory": "client/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
