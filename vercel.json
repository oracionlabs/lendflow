{
  "framework": null,
  "installCommand": "pnpm install",
  "buildCommand": "(cd shared && pnpm run build) && (cd client && pnpm run build)",
  "outputDirectory": "client/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
